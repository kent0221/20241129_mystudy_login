import { memo } from "react";
import { HeaderLayout } from "../templates/HeaderLayout";

export const Setting = memo(() => {
  
  return (
    <HeaderLayout>
      <p>設定ページ</p>
    </HeaderLayout>
  )
});